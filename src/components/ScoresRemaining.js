import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ScoresRemaining = () => {
  return (
    <View style={styles.container}>
      <Text></Text>
    </View>
  );
};

export default ScoresRemaining;

const styles = StyleSheet.create({
  container: {
    width: "100%"
  }
});
